import {describe} from "mocha";
import {expect} from "chai";

describe("Sample", () => {
    describe("Sample test for sample module", () => {
        it("should correctly assert that 1 = 1", () => {
            expect(1).to.equal(1);
        })
    });
});
